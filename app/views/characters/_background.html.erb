<%= form_with(model: character, local: true) do |form| %>
  <% if character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= link_to creator_class_path do, Character.update(@character.id, :background => "Aiel") %>
  <div class="btn btn-default"></div>
  <% end %>

  <%= link_to creator_class_path do, Character.update(@character.id, :background => "Atha'an Miere") %>
  <div class="btn btn-default"></div>
  <% end %>

  <%= link_to creator_class_path do, Character.update(@character.id, :background => "Atha'an Miere") %>
  <div class="btn btn-default"></div>
  <% end %>

<% end %>